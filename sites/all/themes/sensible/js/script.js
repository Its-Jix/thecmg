/**
 * @file
 * sensible.js
 *
 * Provides general enhancements and fixes to Sensible's JS files.
 */

var Drupal = Drupal || {};

(function($, Drupal){
  "use strict";

  Drupal.behaviors.sensible = {
    attach: function(context) {
      //Enhancements
    }
  };
})(jQuery, Drupal);
